<%=
  if presenter.service_ticket_result.valid?

    { :success => {
      :user => presenter.username.to_s,
      :proxyGrantingTicket => presenter.proxy_granting_ticket_iou.to_s,
    }.merge(presenter.extra_attributes)}.to_json

  else

    { :failure => {
      :code => presenter.service_ticket_result.message_category,
      :message => presenter.service_ticket_result.message
    }}.to_json

  end
%>
